# UI 设计

## 窗口

- 默认 1200×700，最小 820×500
- 窗口显式 `with_transparent(false)`，背景清屏色 alpha 固定 255
- 强制 X11 后端（GNOME Wayland 标题栏缺陷，见 [pitfalls.md](pitfalls.md)）
- 深色主题为默认，可切换浅色
- 全局字号比 egui 默认略大：Body 14.5px、Button/Monospace 14px
- 主题使用低饱和中性底色 + 青绿色播放强调色，避免纯默认蓝色控件感
- 中文字体：启动时按候选路径探测加载系统字体
  （Noto Sans CJK → 文泉驿 → DroidSansFallback → arphic），
  注册为 `Proportional` / `Monospace` 的回退字体

## 整体布局

```
┌──────────────────────────────────────────────────────┐
│ 顶栏：♪ 标题 — 艺术家 《专辑》  [+添加][📁文件夹][浅色] │
├──────────────────────────────────────────────────────┤
│ [播放列表] [歌词]              [路径] [编辑/新建歌词] │
│ ┌──────────────────────────────────────────────── ┐ │
│ │ 1  歌曲A                                3:45 [🗑移除] │ │
│ │    歌手                                             │ │
│ │ ▶  歌曲B(青绿色高亮+均衡器)            4:12 [🗑移除] │ │
│ │    歌手 · 《专辑》                                │ │
│ │  …                                              │ │
│ └──────────────────────────────────────────────── ┘ │
├──────────────────────────────────────────────────────┤
│ [⏮][▶][⏹][⏭] [→顺序播放] │ 1:23 ═══════╪═══ 3:45 │🔊│
└──────────────────────────────────────────────────────┘
```

控制栏为**单行紧凑布局**（用户明确要求）：传输按钮 + 模式 + 进度 + 音量，
文件操作和主题切换放顶栏工具栏，「清空」放播放列表页右上角；歌词路径及
“新建/编辑歌词”入口与标签页保持同一行。

### 置顶迷你模式

```
┌──────────────────────────────────────────────────────────────┐
│ [拖动][上一][播放][停止][下一][模式] 时间 音量 [恢复][关闭] │
├──────────────────────────────────────────────────────────────┤
│                  当前歌词（横向渐变推进）                       │
└──────────────────────────────────────────────────────────────┘
```

迷你窗口为 320×60、无系统标题栏且始终置顶；控制行与歌词行各固定 30px。
Pin 状态不显示播放进度条，歌词使用较小字号并保留 18px 左右安全边距。
控制按钮固定为 22×20，在 30px 控制行内垂直居中；传输控制靠左，
音量与窗口控制靠右，中间保留弹性空白。
两行之间的分隔线覆盖绘制，不额外占用高度。所有图标按钮保持固定尺寸，
左侧把手或整行歌词均可拖动窗口；恢复按钮返回进入前的主窗口大小与最大化状态。

## 控制栏细节

- 传输按钮：Material Symbols 图标，统一 `min_size(36×32)` 保证对齐
- 播放/暂停图标 25px，其余 21px（同一按钮框内居中）
- 进度条：
  - 当前时间**右对齐**、总时长**左对齐**，紧贴滑块两端
  - 滑块宽度 = `available_width() - TAIL_W(220)`，TAIL_W 为右侧
    （时间+音量）的预留宽度
  - 拖动中显示 `seek_drag` 预览值，松手（`drag_stopped`）才 seek
- 音量：滑块 82px + 图标（静音时自动切换 volume_off 图标）
- 错误提示条：底部红条 + ✕ 关闭

## 播放列表行

| 元素 | 设计 |
|---|---|
| 行高 | 46px，整行背景铺满列表宽度 |
| 序号列 | 34px 固定宽，等宽字体右对齐 |
| 当前曲 | 4 柱**跳动均衡器**动画（正弦相位错开，播放 20fps / 暂停静止），主题色 |
| 标题 | 15px，当前曲 15.5px 主题色加粗；超长截断省略号 |
| 艺术家 / 专辑 | 第二行弱化显示：`歌手 · 《专辑》` |
| 时长 | 58px 固定宽，等宽字体右对齐 |
| 移除按钮 | 与右键菜单一致：`delete` 图标 + “移除”文案，红色语义，悬停加深；点击后弹出悬浮确认框 |
| 行背景 | 全部使用不透明 RGB 颜色；当前曲为强调底色 + 1px 边线；悬停：`weak_bg_fill`；奇偶行淡底；圆角 6px |
| 清空列表 | 播放列表页右上角红色按钮，不放在底部状态栏；点击后弹出悬浮确认框 |
| 交互 | 整行双击播放、右键菜单；行 hit 区域**排除行尾按钮区**（防点击遮挡） |

### 右键菜单

`▶ 播放` / `🗑 移除`（红字），均带 Material 图标对齐。右键菜单中的“移除”也必须先弹出悬浮确认框，不得直接删除。

### 删除确认

行尾“移除”、右键菜单“移除”和“清空列表”三条交互路径均采用前景居中的悬浮模态二次确认。确认框明确展示操作对象，并提供红色确认按钮与“取消”按钮；点击确认后才执行删除，点击取消、框外区域或按 Escape 均关闭且不修改播放列表。

## 歌词页

见 [lyrics.md](lyrics.md)。

## 图标系统

`egui_material_icons`（Material Symbols 字体）：
- 启动时 `initialize(ctx)` 注册，作为 `Proportional` 回退字体
- 文本内混排：`format!("{} 添加文件", ICON_ADD.codepoint)` 直接生效
- 自绘（painter）：需指定字体族
  `FontFamily::Name(egui_material_icons::FONT_FAMILY.into())`
- 语义图标映射：模式 → `arrow_forward/repeat/repeat_one/shuffle`，
  音量 → `volume_up/volume_off`

## 布局关键经验（egui）

1. egui `Slider` 以 `spacing().slider_width`（默认 100px）自渲染，
   **无视 `add_sized` 宽度** → 必须先 `ui.spacing_mut().slider_width = w`
2. `ui.horizontal` 内容默认垂直居中（`Align::Center`），但按钮框高随内容变化，
   需 `min_size` 统一
3. 后注册的组件在点击测试中位于顶层 → 整行 hit 层要给行内按钮让位
4. `scroll_to_me` 必须在 ScrollArea 闭包内调用
